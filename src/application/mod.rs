// src/application/mod.rs
// 包含應用程序的用例 (use cases) 或應用服務。
// 這一層負責協調 domain 層和 infrastructure 層。
// 例如，一個 `UserService` 可能會使用 `UserRepository` (infrastructure)
// 來操作 `User` 實體 (domain)。

// pub mod user_service;
